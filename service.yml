apiVersion: v1
kind: Service
metadata:
  name: web-service
  namespace: <namespace> # Replace with your desired namespace name
spec:
  selector:
    app: web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: LoadBalancer
